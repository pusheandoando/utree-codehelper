# .github/workflows/release.yml
name: Release

on:
  push:
    branches:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Read version from package.json
        id: read_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Verify branch matches version
        run: |
          BRANCH="${{ github.ref_name }}"
          VERSION="v${{ steps.read_version.outputs.version }}"
          if [ "$BRANCH" != "$VERSION" ]; then
            echo "Branch name '$BRANCH' does not match package.json version '$VERSION'"
            exit 1
          fi

      - name: Check if tag already exists
        id: check_tag
        run: |
          TAG="v${{ steps.read_version.outputs.version }}"
          if git ls-remote --tags origin "$TAG" | grep -q "$TAG"; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Build VSIX
        if: steps.check_tag.outputs.exists == 'false'
        run: |
          chmod +x scripts/build_vsix.sh
          scripts/build_vsix.sh

      - name: Create tag and GitHub Release
        if: steps.check_tag.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.read_version.outputs.version }}
          name: v${{ steps.read_version.outputs.version }}
          generate_release_notes: true
          files: |
            dist/utree-codehelper-${{ steps.read_version.outputs.version }}.vsix